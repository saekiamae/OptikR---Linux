{
  "name": "priority_queue",
  "display_name": "Priority Queue Optimizer",
  "version": "pre-realese-1.0.0",
  "type": "optimizer",
  "target_stage": "pipeline",
  "stage": "global",
  "description": "Prioritizes user-triggered tasks over automatic captures. Improves responsiveness by 20-30%.",
  "author": "OptikR Team",
  "enabled": false,
  "essential": true,
  "pipelines": [
    "visual",
    "audio"
  ],
  "settings": {
    "enable_priorities": {
      "type": "bool",
      "default": true,
      "description": "Enable priority-based scheduling"
    },
    "high_priority_boost": {
      "type": "int",
      "default": 10,
      "min": 1,
      "max": 100,
      "description": "Priority boost for user tasks"
    },
    "max_queue_size": {
      "type": "int",
      "default": 100,
      "min": 10,
      "max": 1000,
      "description": "Maximum queue size"
    },
    "starvation_prevention": {
      "type": "bool",
      "default": true,
      "description": "Prevent low-priority task starvation"
    }
  },
  "performance": {
    "benefit": "20-30% better responsiveness for user tasks",
    "overhead": "Minimal (priority queue management)",
    "memory": "Same as regular queue"
  }
}